%----------------- Rules --------------------
bird(X) :- penguin(X).
[DEFT] fly(X) :- bird(X).
nofly(X) :- penguin(X).

%----------- Negative Constraints -----------
! :- nofly(X), fly(X).

%----------------- Facts --------------------
[DEFT] penguin(tweety).